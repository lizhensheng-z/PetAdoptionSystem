<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yr.pet.ai.mapper.QuestionRecordMapper">


    <select id="queryDayDetails" resultType="com.yr.pet.ai.model.vo.QuestionRecordVO">
        SELECT id,
        user_id       AS userId,
        session_id    AS sessionId,
        question_id   AS questionId,
        type as type,
        req_text      AS reqText,
        resp_text     AS respText,
        create_time    AS createTime
        FROM   ai_question_record
        WHERE  user_id = #{userId}
        AND  date(create_time) = #{day}
        ORDER  BY create_time ASC
    </select>
</mapper>